# SmartRoomVision
SmartRoomVision is a real-time computer vision system that monitors room activity via RTSP camera. It performs face recognition, logs entry/exit times, tracks user behavior, and stores data in a database to generate daily reports.
